version: "3.8"

services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    container_name: backend

  frontend:
    build: ./frontend
    ports:
      - "3001:80"
    depends_on:
      - backend
    container_name: frontend