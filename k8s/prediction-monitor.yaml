apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: prediction-monitor
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: prediction
  endpoints:
  - port: http
    path: /metrics
    interval: 15s
